<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('home') }}">SymEcommerce</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">

      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
            <a class="nav-link" aria-current="page" href="{{ path('admin_home') }}">Administration</a>
        </li>

        {% for category in categoryRepository.findAll() %}

            <li class="nav-item">
              <a class="nav-link" aria-current="page" href="{{ path('boutique_product_show_by_category',{'id':category.id}) }}">{{ category.name }}</a>
            </li>
        {% endfor %}

        </ul>


        </div>
    </div>
</nav>